<?xml version="1.0"?>
<Entity
  name="CPBuildingCovOtherThanHurricaneCost">
  <Columns>
    <Column
      beanPath="CPBuildingCovOtherThanHurricaneCost.CPBuildingCov"
      name="coverage"/>
    <Column
      beanPath="CPBuildingCovOtherThanHurricaneCost.CostType"
      name="costType"/>
  </Columns>
  <DisplayName><![CDATA[if(costType != null)
  return coverage + " Other Than Hurricane : " + costType.DisplayName
else
  return coverage + " Other Than Hurricane"]]></DisplayName>
</Entity>