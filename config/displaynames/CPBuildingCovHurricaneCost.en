<?xml version="1.0"?>
<Entity
  name="CPBuildingCovHurricaneCost">
  <Columns>
    <Column
      beanPath="CPBuildingCovHurricaneCost.CPBuildingCov"
      name="coverage"/>
    <Column
      beanPath="CPBuildingCovHurricaneCost.CostType"
      name="costType"/>
  </Columns>
  <DisplayName><![CDATA[if(costType != null)
  return coverage + " Hurricane : " + costType.DisplayName
else
  return coverage + " Hurricane"]]></DisplayName>
</Entity>