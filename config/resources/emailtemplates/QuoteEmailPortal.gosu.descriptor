<?xml version="1.0" encoding="UTF-8"?>

<serialization>

  <emailtemplate-descriptor

    name="Quote Email Portal"
    keywords="quote, email"
    topic="request"
    subject="Insurance quote #&lt;%= sub.JobNumber %&gt;"
    body="QuoteEmailPortal.gosu"
    requiredsymbols="sub, qdd, sender, quoteUrl, contactNumber, chatURL"

  />

</serialization>